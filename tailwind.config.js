module.exports = {
  purge: ['./src/**/*.ts', './src/**/*.tsx'],
  darkMode: 'class',
  variants: {
    extend: {
      cursor: ['disabled'],
      opacity: ['disabled'],
      textOpacity: ['disabled'],
    },
  }

};
