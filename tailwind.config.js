module.exports = {
  purge: ['./src/**/*.ts', './src/**/*.tsx'],
  darkMode: 'media',
  variants: {
    extend: {
      cursor: ['disabled'],
      opacity: ['disabled'],
      textOpacity: ['disabled'],
    },
  }

};
